# config.py — การตั้งค่าส่วนกลางของ Dashboard
# แก้ไขค่าต่างๆ ในไฟล์นี้เพื่อปรับแต่งระบบ

import os

# =========================================================
# PATH
# =========================================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DATA_FOLDER = os.path.join(BASE_DIR, "data", "raw")

# =========================================================
# PERIOD (ปีงบประมาณ พ.ศ.)
# =========================================================
PERIOD = "2568"

# =========================================================
# ZONES — เขตสินเชื่อ (แก้ไขให้ตรงกับหน่วยงานจริง)
# =========================================================
ZONES = [
    "เขตสินเชื่อ 1",
    "เขตสินเชื่อ 2",
    "เขตสินเชื่อ 3",
    "เขตสินเชื่อ 4",
    "เขตสินเชื่อ 5",
]

# =========================================================
# CATEGORIES — หมวดผลการดำเนินงาน
# =========================================================
CATEGORIES = ["สินเชื่อ", "เงินฝาก", "NPL", "ประกัน", "ผลิตภัณฑ์"]

# Mapping: category → file name
CATEGORY_FILES = {
    "สินเชื่อ":    "loan_data.xlsx",
    "เงินฝาก":    "deposit_data.xlsx",
    "NPL":        "npl_data.xlsx",
    "ประกัน":     "insurance_data.xlsx",
    "ผลิตภัณฑ์":  "product_data.xlsx",
}

# สำหรับ NPL ยิ่งน้อยยิ่งดี (lower_is_better = True)
LOWER_IS_BETTER = {"NPL"}

# =========================================================
# THRESHOLDS — เกณฑ์การประเมินสถานะ (%)
# =========================================================
THRESHOLDS = {
    "green":  100,   # achievement_rate >= 100 → บรรลุเป้า
    "yellow":  80,   # 80 <= rate < 100        → ใกล้เป้า
    "red":      0,   # rate < 80               → ต่ำกว่าเป้า
}

# =========================================================
# COLORS
# =========================================================
COLOR_MAP = {
    "บรรลุเป้า":    "#28a745",   # green
    "ใกล้เป้า":     "#ffc107",   # yellow
    "ต่ำกว่าเป้า":  "#dc3545",   # red
}

STATUS_EMOJI = {
    "บรรลุเป้า":    "✅",
    "ใกล้เป้า":     "⚠️",
    "ต่ำกว่าเป้า":  "🔴",
}

# =========================================================
# BRANCH INFO
# =========================================================
BRANCH_NAME = "ธ.ก.ส. หน่วยลำพญากลาง"
BRANCH_CODE = "69"
